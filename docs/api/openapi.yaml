openapi: 3.1.0
info:
  title: zip02 API
  version: 1.0.0
  description: MVP API for organizer events, ticket reservation, and Stripe-style payment webhooks.
servers:
  - url: https://localhost:5001
paths:
  /events:
    $ref: './paths/events/create.yaml'
  /events/{id}:
    $ref: './paths/events/item.yaml'
  /tickets/reserve:
    $ref: './paths/tickets/reserve.yaml'
  /tickets/{id}:
    $ref: './paths/tickets/item.yaml'
  /tickets/expire-reservations:
    $ref: './paths/tickets/expire-reservations.yaml'
  /payments/stripe/webhook:
    $ref: './paths/payments/stripe-webhook.yaml'
  /checkin:
    $ref: './paths/checkin.yaml'
  /refunds/events/{eventId}/reconcile:
    $ref: './paths/refunds/reconcile.yaml'
  /refunds/tickets/{ticketId}:
    $ref: './paths/refunds/manual-ticket.yaml'
components:
  schemas:
    $ref: './components/schemas.yaml#/schemas'
