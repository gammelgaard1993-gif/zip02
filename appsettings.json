{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Payments": {
    "Stripe": {
      // Set via environment variable or user secrets — never commit a real secret here.
      "WebhookSecret": ""
    }
  },
  "DynamoDB": {
    // Leave TableName empty to run the app with the in-memory store (no AWS needed).
    // Set it to the real table name in staging/production via environment variables:
    //   DynamoDB__TableName=zip02-tickets
    // For LocalStack add:
    //   AWS__ServiceURL=http://localhost:4566
    //   AWS__Region=eu-west-1
    "TableName": ""
  },
  "AllowedHosts": "*"
}
